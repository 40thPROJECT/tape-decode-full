name: Build and release binary

on:
  workflow_dispatch:
    inputs:
      create_release:
        description: "Create a GitHub release and upload built assets"
        required: false
        type: boolean
        default: false
      release_tag:
        description: "Tag to release when manually dispatched (example: v0.6.0)"
        required: false
        type: string
  push:
    tags:
      - "v*"

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  windows-decode:
    name: Build tape-decode-full-gui EXE (x86_64)
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Resolve build version
        id: version
        shell: bash
        run: |
          set -euo pipefail
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            VERSION="${GITHUB_REF#refs/tags/}"
          elif [[ "${{ github.event_name }}" == "workflow_dispatch" && -n "${{ github.event.inputs.release_tag }}" ]]; then
            VERSION="${{ github.event.inputs.release_tag }}"
          else
            VERSION="$(git describe --tags --always --dirty)"
          fi
          VERSION="${VERSION#v}"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Build tape-decode core (Windows x86-64 v1-v4, for bundling)
        shell: bash
        run: |
          set -euo pipefail
          build_level() {
            local level="$1"
            local rustflags=""
            case "$level" in
              v1) rustflags="-C target-feature=+sse2" ;;
              v2) rustflags="-C target-feature=+sse3,+ssse3,+sse4.1,+sse4.2" ;;
              v3) rustflags="-C target-feature=+avx,+avx2" ;;
              v4) rustflags="-C target-feature=+avx512f,+avx512bw,+avx512cd,+avx512dq,+avx512vl" ;;
            esac
            echo "::group::Build Windows ${level}"
            CARGO_TARGET_DIR="target-x86-64-${level}" RUSTFLAGS="$rustflags" \
              cargo build --release --target x86_64-pc-windows-msvc --bin tape-decode-full
            echo "::endgroup::"
          }
          for level in v1 v2 v3 v4; do
            build_level "$level"
          done

      - name: List built Windows core binaries (for bundling)
        shell: bash
        run: |
          set -euo pipefail
          ls -la target-x86-64-v*/x86_64-pc-windows-msvc/release/tape-decode-full.exe 2>/dev/null || true
          ls -la target/x86_64-pc-windows-msvc/release/tape-decode-full.exe 2>/dev/null || true

      - name: Test Rust core
        shell: bash
        run: cargo test --workspace --release

      - name: Smoke test raw tape-decode binary (Windows)
        shell: bash
        run: |
          set -euo pipefail
          # Prefer a level binary (lowest first) so it runs on any x86-64 host
          BIN=""
          for lvl in v1 v2 v3 v4; do
            p="target-x86-64-${lvl}/x86_64-pc-windows-msvc/release/tape-decode-full.exe"
            if [ -f "$p" ]; then BIN="$p"; break; fi
          done
          if [ -z "$BIN" ]; then BIN="target/x86_64-pc-windows-msvc/release/tape-decode-full.exe"; fi
          if [ ! -f "$BIN" ]; then
            echo "No tape-decode-full.exe found for smoke test"; ls -la target*/x86_64-pc-windows-msvc/release/ || true; exit 1
          fi
          echo "Smoking: $BIN"
          TAPE_DECODE_MICROARCH=x86-64-v1 "$BIN" list-profiles | tee /tmp/tape_decode_profiles.txt
          grep -q "PAL_VHS" /tmp/tape_decode_profiles.txt

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install launcher packaging dependencies
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          pip install pyinstaller pyinstaller-versionfile -r requirements-launcher.txt

      - name: Build decode launcher (Windows x86-64 v1-v4)
        shell: bash
        run: python scripts/ci/build-windows-decode-bin.py

      - name: Package tape-decode-full-gui Windows artifact
        shell: bash
        run: |
          set -euo pipefail
          VERSION="${{ steps.version.outputs.version }}"
          EXE="dist/tape-decode-full-gui.exe"
          if [ ! -f "$EXE" ]; then
            echo "tape-decode-full-gui.exe not produced"
            ls -la dist/ || true
            exit 1
          fi
          cp "$EXE" "tape-decode-full-gui-windows_${VERSION}_x86_64.exe"

      - name: Upload tape-decode-full-gui Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: tape-decode-full-gui-windows-x86_64
          path: |
            tape-decode-full-gui-windows_*_x86_64.exe
          if-no-files-found: warn

  linux-decode:
    name: Linux tape-decode-full-gui AppImage (${{ matrix.arch }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            runner: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
            appimagetool: appimagetool-x86_64.AppImage
            appimage_arch: x86_64
            artifact_suffix: x86_64
          - arch: arm64
            runner: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
            appimagetool: appimagetool-aarch64.AppImage
            appimage_arch: aarch64
            artifact_suffix: arm64
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Resolve build version
        id: version
        run: |
          set -euo pipefail
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            VERSION="${GITHUB_REF#refs/tags/}"
          elif [[ "${{ github.event_name }}" == "workflow_dispatch" && -n "${{ github.event.inputs.release_tag }}" ]]; then
            VERSION="${{ github.event.inputs.release_tag }}"
          else
            VERSION="$(git describe --tags --always --dirty)"
          fi
          VERSION="${VERSION#v}"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Build tape-decode core (single, for bundling)
        if: matrix.arch != 'x86_64'
        run: cargo build --release --target "${{ matrix.target }}" --bin tape-decode-full

      - name: Build tape-decode core (x86-64 v1-v4 levels, for bundling)
        if: matrix.arch == 'x86_64'
        run: |
          set -euo pipefail
          build_level() {
            local level="$1"
            local rustflags=""
            case "$level" in
              v1) rustflags="-C target-feature=+sse2" ;;
              v2) rustflags="-C target-feature=+sse3,+ssse3,+sse4.1,+sse4.2" ;;
              v3) rustflags="-C target-feature=+avx,+avx2" ;;
              v4) rustflags="-C target-feature=+avx512f,+avx512bw,+avx512cd,+avx512dq,+avx512vl" ;;
            esac
            echo "::group::Build ${level}"
            CARGO_TARGET_DIR="target-x86-64-${level}" RUSTFLAGS="$rustflags" \
              cargo build --release --target "${{ matrix.target }}" --bin tape-decode-full
            echo "::endgroup::"
          }
          for level in v1 v2 v3 v4; do
            build_level "$level"
          done

      - name: Test Rust core
        run: cargo test --workspace --release

      - name: Smoke test raw tape-decode binary
        shell: bash
        run: |
          set -euo pipefail
          if [[ "${{ matrix.arch }}" == "x86_64" ]]; then
            BIN=""
            for lvl in v1 v2 v3 v4; do
              p="target-x86-64-${lvl}/x86_64-unknown-linux-gnu/release/tape-decode-full"
              if [ -f "$p" ]; then BIN="$p"; break; fi
            done
            if [ -z "$BIN" ]; then BIN="target/x86_64-unknown-linux-gnu/release/tape-decode-full"; fi
            if [ ! -f "$BIN" ]; then
              echo "No tape-decode binary found for smoke"; ls -la target*/x86_64-unknown-linux-gnu/release/ || true; exit 1
            fi
            echo "Smoking: $BIN"
            TAPE_DECODE_MICROARCH=x86-64-v1 "$BIN" list-profiles | tee /tmp/tape_decode_profiles.txt
            grep -q "PAL_VHS" /tmp/tape_decode_profiles.txt
          else
            BIN="target/aarch64-unknown-linux-gnu/release/tape-decode-full"
            if [ ! -f "$BIN" ]; then
              echo "No tape-decode binary found for smoke"; ls -la target/aarch64-unknown-linux-gnu/release/ || true; exit 1
            fi
            echo "Smoking: $BIN"
            "$BIN" list-profiles | tee /tmp/tape_decode_profiles.txt
            grep -q "PAL_VHS" /tmp/tape_decode_profiles.txt
          fi

      - name: Setup Python (for decode launcher)
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install system libraries needed for complete PyQt6 xcb plugin collection
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq \
            libxcb-cursor0 \
            libxkbcommon-x11-0 \
            libxcb-icccm4 libxcb-image0 libxcb-keysyms1 libxcb-randr0 \
            libxcb-render0 libxcb-render-util0 libxcb-shape0 libxcb-shm0 \
            libxcb-sync1 libxcb-xfixes0 libxcb-xinerama0 libxcb-xkb1 \
            libxkbcommon0 || true

      - name: Install launcher packaging dependencies
        run: |
          python3 -m pip install --upgrade pip
          pip install pyinstaller -r requirements-launcher.txt

      - name: Build launcher binary
        if: matrix.arch != 'x86_64'
        env:
          TAPE_DECODE_BIN: target/${{ matrix.target }}/release/tape-decode-full
        run: python3 scripts/ci/build-linux-decode-bin.py

      - name: Build launcher binary (with microarch levels)
        if: matrix.arch == 'x86_64'
        run: python3 scripts/ci/build-linux-decode-bin.py

      - name: Build AppDir and AppImage
        env:
          BUILD_VERSION: ${{ steps.version.outputs.version }}
        shell: bash
        run: |
          set -euo pipefail

          rm -rf AppDir squashfs-root
          mkdir -p AppDir/usr/bin \
            AppDir/usr/share/applications \
            AppDir/usr/share/icons/hicolor/256x256/apps \
            AppDir/resources/icon

          cp dist/tape-decode-full-gui AppDir/usr/bin/tape-decode-full-gui
          chmod +x AppDir/usr/bin/tape-decode-full-gui

          cp resources/appimage/tape-decode-full-gui.desktop AppDir/usr/share/applications/tape-decode-full-gui.desktop
          cp resources/appimage/AppRun AppDir/AppRun
          chmod +x AppDir/AppRun

          cp resources/icon/tape-decode-full-256.png AppDir/usr/share/icons/hicolor/256x256/apps/tape-decode-full-gui.png

          # Keep one canonical icon file and expose required aliases via symlink to avoid duplicate payload files.
          ln -sf usr/share/icons/hicolor/256x256/apps/tape-decode-full-gui.png AppDir/tape-decode-full-gui.png
          ln -sf tape-decode-full-gui.png AppDir/.DirIcon
          ln -sf ../../tape-decode-full-gui.png AppDir/resources/icon/tape-decode-full-256.png
          ln -sf tape-decode-full-gui.png AppDir/tape-decode-full-256.png

          ln -sf usr/share/applications/tape-decode-full-gui.desktop AppDir/tape-decode-full-gui.desktop

          APPIMAGE_TOOL="${{ matrix.appimagetool }}"
          APPIMAGE_NAME="tape-decode-full-gui-linux_${BUILD_VERSION}_${{ matrix.artifact_suffix }}.AppImage"

          wget -q "https://github.com/AppImage/AppImageKit/releases/download/continuous/${APPIMAGE_TOOL}"
          chmod +x "${APPIMAGE_TOOL}"
          "./${APPIMAGE_TOOL}" --appimage-extract
          env -u SOURCE_DATE_EPOCH ARCH=${{ matrix.appimage_arch }} ./squashfs-root/AppRun AppDir "${APPIMAGE_NAME}"
          chmod +x "${APPIMAGE_NAME}"

          # Only force a microarch level on x86-64 builds. On arm64 the bundled binary is aarch64.
          if [[ "${{ matrix.arch }}" == "x86_64" ]]; then
            TAPE_DECODE_MICROARCH=x86-64-v1 APPIMAGE_EXTRACT_AND_RUN=1 "./${APPIMAGE_NAME}" list-profiles > /tmp/tape-decode-full-gui_profiles.txt || true
          else
            APPIMAGE_EXTRACT_AND_RUN=1 "./${APPIMAGE_NAME}" list-profiles > /tmp/tape-decode-full-gui_profiles.txt || true
          fi
          grep -q "PAL_VHS" /tmp/tape-decode-full-gui_profiles.txt || true

          ZIP_NAME="tape-decode-full-gui-linux_${BUILD_VERSION}_${{ matrix.artifact_suffix }}.zip"
          zip -j "${ZIP_NAME}" "${APPIMAGE_NAME}"
          test -f "${ZIP_NAME}"
          test "$(unzip -Z1 "${ZIP_NAME}" | wc -l)" -eq 1

      - name: Upload tape-decode-full-gui Linux artifact
        uses: actions/upload-artifact@v4
        with:
          name: tape-decode-full-gui-linux-${{ matrix.artifact_suffix }}
          path: |
            tape-decode-full-gui-linux_*_${{ matrix.artifact_suffix }}.zip
          if-no-files-found: warn

  macos-decode:
    name: macOS tape-decode-full-gui DMG (${{ matrix.arch }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            runner: macos-15-intel
            target: x86_64-apple-darwin
            artifact_suffix: x86_64
          - arch: arm64
            runner: macos-latest
            target: aarch64-apple-darwin
            artifact_suffix: arm64
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Resolve build version
        id: version
        run: |
          set -euo pipefail
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            VERSION="${GITHUB_REF#refs/tags/}"
          elif [[ "${{ github.event_name }}" == "workflow_dispatch" && -n "${{ github.event.inputs.release_tag }}" ]]; then
            VERSION="${{ github.event.inputs.release_tag }}"
          else
            VERSION="$(git describe --tags --always --dirty)"
          fi
          VERSION="${VERSION#v}"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Build tape-decode core (single, for bundling)
        if: matrix.arch != 'x86_64'
        run: cargo build --release --target "${{ matrix.target }}" --bin tape-decode-full

      - name: Build tape-decode core (x86-64 v1-v4 levels, for bundling)
        if: matrix.arch == 'x86_64'
        run: |
          set -euo pipefail
          build_level() {
            local level="$1"
            local rustflags=""
            case "$level" in
              v1) rustflags="-C target-feature=+sse2" ;;
              v2) rustflags="-C target-feature=+sse3,+ssse3,+sse4.1,+sse4.2" ;;
              v3) rustflags="-C target-feature=+avx,+avx2" ;;
              v4) rustflags="-C target-feature=+avx512f,+avx512bw,+avx512cd,+avx512dq,+avx512vl" ;;
            esac
            echo "::group::Build ${level}"
            CARGO_TARGET_DIR="target-x86-64-${level}" RUSTFLAGS="$rustflags" \
              cargo build --release --target "${{ matrix.target }}" --bin tape-decode-full
            echo "::endgroup::"
          }
          for level in v1 v2 v3 v4; do
            build_level "$level"
          done

      - name: Test Rust core
        run: cargo test --workspace --release

      - name: Smoke test raw tape-decode binary
        shell: bash
        run: |
          set -euo pipefail
          if [[ "${{ matrix.arch }}" == "x86_64" ]]; then
            BIN=""
            for lvl in v1 v2 v3 v4; do
              p="target-x86-64-${lvl}/x86_64-apple-darwin/release/tape-decode-full"
              if [ -f "$p" ]; then BIN="$p"; break; fi
            done
            if [ -z "$BIN" ]; then BIN="target/x86_64-apple-darwin/release/tape-decode-full"; fi
            if [ ! -f "$BIN" ]; then
              echo "No tape-decode binary found for smoke"; ls -la target*/x86_64-apple-darwin/release/ || true; exit 1
            fi
            echo "Smoking: $BIN"
            TAPE_DECODE_MICROARCH=x86-64-v1 "$BIN" list-profiles | tee /tmp/tape_decode_profiles.txt
            grep -q "PAL_VHS" /tmp/tape_decode_profiles.txt
          else
            BIN="target/aarch64-apple-darwin/release/tape-decode-full"
            if [ ! -f "$BIN" ]; then
              echo "No tape-decode binary found for smoke"; ls -la target/aarch64-apple-darwin/release/ || true; exit 1
            fi
            echo "Smoking: $BIN"
            "$BIN" list-profiles | tee /tmp/tape_decode_profiles.txt
            grep -q "PAL_VHS" /tmp/tape_decode_profiles.txt
          fi

      - name: Setup Python (for decode launcher)
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install launcher packaging dependencies
        run: |
          python3 -m pip install --upgrade pip
          pip install pyinstaller -r requirements-launcher.txt

      - name: Build launcher binary
        if: matrix.arch != 'x86_64'
        env:
          TAPE_DECODE_BIN: target/${{ matrix.target }}/release/tape-decode-full
        run: python3 scripts/ci/build-macos-decode-bin.py

      - name: Build launcher binary (with microarch levels)
        if: matrix.arch == 'x86_64'
        run: python3 scripts/ci/build-macos-decode-bin.py

      - name: Ad-hoc sign app bundle
        run: codesign -s - --deep --force dist/tape-decode-full-gui.app

      - name: Package tape-decode-full-gui macOS DMG
        env:
          BUILD_VERSION: ${{ steps.version.outputs.version }}
        shell: bash
        run: |
          set -euo pipefail
          DMG_NAME="tape-decode-full-gui-macos_${BUILD_VERSION}_${{ matrix.artifact_suffix }}.dmg"
          rm -rf build/dmg
          mkdir -p build/dmg
          cp -R dist/tape-decode-full-gui.app build/dmg/
          ln -s /Applications build/dmg/Applications
          hdiutil create -volname "tape-decode-full-gui" -srcfolder build/dmg -ov -format UDZO "$DMG_NAME"
          test -f "$DMG_NAME"

          # smoke (force v1 on x86-64, none on arm)
          if [[ "${{ matrix.arch }}" == "x86_64" ]]; then
            TAPE_DECODE_MICROARCH=x86-64-v1 dist/tape-decode-full-gui.app/Contents/MacOS/tape-decode-full-gui list-profiles > /tmp/tape-decode-full-gui_profiles.txt || true
          else
            dist/tape-decode-full-gui.app/Contents/MacOS/tape-decode-full-gui list-profiles > /tmp/tape-decode-full-gui_profiles.txt || true
          fi
          grep -q "PAL_VHS" /tmp/tape-decode-full-gui_profiles.txt || true

      - name: Upload tape-decode-full-gui macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: tape-decode-full-gui-macos-${{ matrix.artifact_suffix }}
          path: tape-decode-full-gui-macos_*_${{ matrix.artifact_suffix }}.dmg
          if-no-files-found: warn

  release:
    name: Create GitHub release
    needs: [windows-decode, linux-decode, macos-decode]
    if: github.event_name == 'workflow_dispatch' && inputs.create_release == true
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Resolve build version
        id: version
        run: |
          set -euo pipefail
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            VERSION="${GITHUB_REF#refs/tags/}"
          elif [[ "${{ github.event_name }}" == "workflow_dispatch" && -n "${{ github.event.inputs.release_tag }}" ]]; then
            VERSION="${{ github.event.inputs.release_tag }}"
          else
            VERSION="$(git describe --tags --always --dirty)"
          fi
          VERSION="${VERSION#v}"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.version }}
          name: "tape-decode-full-gui v${{ steps.version.outputs.version }}"
          draft: true
          body: |
            ## Decode Launcher (tape-decode-full-gui)

            This release ships the Decode Launcher ("tape-decode-full-gui") with built-in support for x86-64 microarchitecture level selection (Auto / v1 / v2 / v3 / v4).

            The tape-decode-full-gui binary is the primary artifact. It provides the GUI when launched normally, and full CLI functionality when invoked from a terminal (e.g. `tape-decode-full-gui list-profiles`, `tape-decode-full-gui decode ...`). No separate raw tape-decode binary is released.

            ### Included
            - tape-decode-full-gui launcher for:
              - Windows (x86-64)
              - Linux (x86-64 + arm64 AppImage, packaged in ZIP)
              - macOS (x86-64 + arm64)
            - 5 GUI artifacts total: 1 Windows exe, 2 Linux ZIPs, 2 macOS DMGs.
            - The tape-decode core implementation is bundled inside (v1 default + v2/v3/v4 level-optimized variants for x86-64 where applicable).
            - Profiles list and list-profiles verification included.

            ### x86-64 Microarchitecture Levels
            - Auto detects best available.
            - Explicit levels use targeted RUSTFLAGS:
              - v1: `-C target-feature=+sse2`
              - v2: `-C target-feature=+sse3,+ssse3,+sse4.1,+sse4.2`
              - v3: `-C target-feature=+avx,+avx2`
              - v4: `-C target-feature=+avx512f,+avx512bw,+avx512cd,+avx512dq,+avx512vl`

            ## Auto-generated notes
          generate_release_notes: true
          append_body: true
          files: |
            artifacts/tape-decode-full-gui-windows-x86_64/*.exe
            artifacts/tape-decode-full-gui-linux-x86_64/*.zip
            artifacts/tape-decode-full-gui-linux-arm64/*.zip
            artifacts/tape-decode-full-gui-macos-x86_64/*.dmg
            artifacts/tape-decode-full-gui-macos-arm64/*.dmg
